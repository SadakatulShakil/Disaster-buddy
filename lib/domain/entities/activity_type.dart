/// Which kind of cross-cutting [Activity] this is — determines which typed
/// [ActivityContent] subtype it carries and which presentation page/
/// controller drives it.
enum ActivityType { kitBuilder, signalColours, safeSpotFinder, readTheSky }
